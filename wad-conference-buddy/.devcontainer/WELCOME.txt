  ╭──────────────────────────────────────────────────────────────╮
  │  Conference Buddy — building agents with deepagents          │
  │  WeAreDevelopers World Congress North America 2026           │
  ╰──────────────────────────────────────────────────────────────╯

  If you can read this box, the dev container set itself up correctly:
  Python, Jupyter, the dependencies and the dataset are all in place.

  1.  Open  conference_buddy.ipynb
  2.  Run the first cell. The kernel should be picked for you; if you
      are asked, choose  Python 3.11  at  /usr/local/bin/python
  3.  Keep going in order.

  Didn't see this box, or the notebook has no kernel?
      Command Palette (F1) → "Codespaces: Rebuild Container"